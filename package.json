{
  "name": "napi-router",
  "version": "1.0.0",
  "description": "Bun-compatible HTTP server powered by Rust via NAPI — works in Node.js, Bun, and Deno",
  "keywords": [
    "napi",
    "native",
    "rust",
    "http",
    "server",
    "websocket",
    "bun-compatible",
    "bun",
    "hono"
  ],
  "homepage": "https://github.com/your-username/your-repo#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/your-username/your-repo.git"
  },
  "license": "MIT",
  "type": "module",
  "main": "index.js",
  "types": "index.d.ts",
  "exports": {
    ".": {
      "require": "./index.js",
      "default": "./index.js"
    },
    "./adapter": {
      "import": "./adapter/serve.ts",
      "default": "./adapter/serve.ts"
    },
    "./adapter/serve": {
      "import": "./adapter/serve.ts",
      "default": "./adapter/serve.ts"
    }
  },
  "files": [
    "index.js",
    "index.d.ts",
    "*.node",
    "adapter/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "napi build --platform --release",
    "build:debug": "napi build --platform",
    "clean": "napi clean && cargo clean",
    "test": "bun test",
    "dev": "node --watch examples/dev.ts",
    "bench": "bun bench/compare.ts",
    "bench:simple": "bun bench/server.ts",
    "type-check": "tsc --noEmit",
    "format": "prettier --write index.js index.d.ts adapter/**/*.ts examples/",
    "lint": "rustup run stable cargo clippy -- -D warnings"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.5.1",
    "typescript": "^5.6.2"
  },
  "napi": {
    "binaryName": "napi-router",
    "targets": [
      "x86_64-pc-windows-msvc",
      "i686-pc-windows-msvc",
      "x86_64-apple-darwin",
      "aarch64-apple-darwin",
      "x86_64-unknown-linux-gnu",
      "aarch64-unknown-linux-gnu"
    ]
  },
  "dependencies": {
    "hono": "^4.12.22"
  }
}
